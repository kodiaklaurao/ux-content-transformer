[build]
  publish = "."
  functions = "netlify/functions"

# Same-origin URL the page calls (no vendor name in the client).
[[redirects]]
  from = "/api/microcopy"
  to = "/.netlify/functions/microcopy"
  status = 200
